# Changelog

本项目的版本变更记录。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Changed

- 保持应用版本为 1.1.5，将新用户的默认阅读参数调整为：楷体、常规、22pt、行距 8、左右边距 35、上边距 72、下边距 24、仿真翻页。
- 重构探索长列表的封面加载管线：磁盘解码与网络分别限流、可见图片优先、离屏请求可取消，并将批量完成的图片状态分散到相邻显示帧。
- 探索分页不再一次性预热整批新封面，改由接近视口的卡片按需、延迟加载；书架启动预热同步缩减为 4 张。
- 探索页书目 ID 改为随分页增量维护，并排除不需要持久化的加载状态，减少新页接入时的主线程扫描和 JSON 写入。

### Fixed

- 修复滚动阅读模式到达章末后继续上滑无法进入下一章的问题；现在需从章末开始一次新的上滑，超过阈值后切换，避免惯性滚动误触。
- 降低“查看全部”快速下滑时大量封面并发下载、读盘、解码和纹理提交造成的瞬时掉帧。
- 修复探索“查看全部”在横屏/大屏下两行书名挤占作者区域、同一行文字基线不整齐的问题。
- 修复 iOS 16 下滚动阅读器的 SwiftUI 拖动手势被原生滚动容器取消、导致章末继续上滑不切章的问题；改为直接观察现有 UIScrollView 手势，并兼容不足一屏的短章节。

### Tests

- 增加封面加载节流/优先级、缓存目录创建和探索分页增量去重状态测试。

## [1.1.5] - 2026-09-03

### Changed

- 拟真翻页改为 Apple Books 风格的单面纸张模型，纸背由 UIKit 从正文页快照生成，保留低调的文字透印并移除独立黑色纸背造成的强烈灰白高光。
- 点按和滑动翻页统一为纯正文页索引序列，不再在人为的 `front` / `back` surface 之间交替。

### Fixed

- 修复 `isDoubleSided = true` 时程序化点按只传入一个控制器，触发“provided (1), required (2)”异常而导致左右点按闪退的问题。
- 删除 v1.1.2 与 v1.1.4 之间静止状态需要一个控制器、双面过渡需要两个控制器的互斥配置，从根源消除两类数量异常。

### Tests

- 增加单面配置、正文页直连序列、前后数据源和双方向单控制器程序化切页回归测试。

## [1.1.4] - 2026-09-03

### Changed

- 拟真翻页的反向点击改为延迟到手势回调结束后执行，并换用全新的目标控制器，通过无内部动画的正向设置与短淡化过渡完全绕开 UIKit 反向代码动画队列；左右滑动仍使用系统卷页。
- 纸背改由完全不透明的 `MattePaperView` 使用 copy 混合模式绘制纯色，清除应用层阴影、边框、栅格化和合成滤镜。

### Fixed

- 恢复探索书库封面 16pt 连续圆角，移除固定尺寸之后的矩形裁切。
- 修复点击阅读页左侧时，反向 `setViewControllers` 复用 UIKit 内部仍持有的旧页面而导致的闪退。
- 跨章节点击延迟到手势派发结束后执行，避免切章同步销毁当前 page controller。

### Tests

- 增加探索封面圆角、哑光纸背图层属性和反向点击安全策略测试。

## [1.1.3] - 2026-09-02

### Changed

- 探索书库从逐行动态测量的 `LazyVGrid` 改为固定列宽、固定卡片高度的显式等高行，分页追加时保留既有行身份。
- 分页预取改为每行最多创建一个任务，加载提示与触底哨兵使用相同固定高度。

### Fixed

- 修复探索列表越过两行接缝时内容偏移被校正、体感吸附到上一行的问题。
- 修复拟真翻页启用双面纸张后，`UIPageViewController` 在左侧书脊模式被传入两个静止页面而导致阅读页立即闪退的问题。
- 保留由数据源按需提供的同色纸背，不再把纸背错误地当作第二个静止页面提交给 UIKit。

### Tests

- 增加探索等高行分组、固定几何尺寸以及 page-curl 单控制器静止状态回归测试。

## [1.1.2] - 2026-09-02

### Changed

- 探索书库网格改为稳定固定列宽，分页预取由末尾多个单元格重复触发改为单一触发点。
- 阅读背景主题与 App 外观彻底解耦；两处「跟随系统」分别直接依据 iOS 系统明暗外观。
- 拟真翻页启用 `UIPageViewController` 双面纸张模型，显式提供与正面完全同色的纸背。

### Fixed

- 移除探索下一页 12 个占位书卡与真实书卡之间的批量替换，消除页间网格重排顿挫。
- 修复隐藏阅读上下栏后 safe area 改变导致正文上移、底部出现空带的问题。
- 修复深色阅读背景下拟真翻页纸张背面仍显示为浅色的问题。

### Tests

- 增加探索单触发点与固定网格尺寸、阅读系统主题解析、拟真翻页正背面序列及纸背颜色测试。

## [1.1.1] - 2026-09-02

### Changed

- 启动更新检查改为后台调度，并将书架目录检查限制为最多 2 路并发。
- 目录加载增加同请求合并，避免详情页切换和视图任务重复拉取同一本书。
- 阅读分页支持协作取消；翻页缓存使用真实 LRU，并为动画状态增加超时解锁。
- 封面内存缓存改为命中提升的 LRU，磁盘键改用稳定 SHA-256 并兼容迁移旧缓存。

### Fixed

- 离线插图文件名不再依赖跨进程随机的 `String.hashValue`，旧缓存按需迁移。
- 移除在 POSIX 信号处理器中调用 Foundation 和文件 I/O 的不安全崩溃记录逻辑。
- 避免启动路径同步遍历封面缓存目录。
- 修复探索「查看全部」后无法继续加载：后续页改用具备 HTTP/重定向校验的 Swift 请求层，拒绝空页和重复页伪成功。
- 修复探索缓存恢复后页码归零、后续页错误被静默吞掉的问题，并提供可重试的底部错误提示。
- 修复系统 LaunchScreen 切换到应用内开屏时，底部 Logo 与文字因动态高度和 safe area 变化产生的跳动。
- 强调色默认值和异常回退统一为蓝色，设置页勾选状态不再错误落在浅紫色。
- 应用内版本信息改为读取构建版本，并统一显示为 `Version 1.1.1`。

### Tests

- 增加稳定缓存键、旧离线插图迁移、分页取消和目录请求去重测试。
- 增加探索分页追加、重复页拒绝和后续页失败重试测试。
- 增加开屏几何尺寸、强调色默认值和版本显示格式回归测试。

## [1.1.0] - 2026-08-25

### Added

- **KMP 数据互通层（SharedKit）**：新增 `shared/` Kotlin Multiplatform 模块，
  从上游 [LightNovelReader](https://github.com/dmzz-yyhyy/LightNovelReader) 移植数据模型
  （`BookInformation`/`BookVolumes`/`ChapterContent`/`UserReadingData` 等）与 wenku8 抓取器，
  编译为 `SharedKit.xcframework` 供 iOS 侧调用。书 ID、组件 JSON 格式与上游完全一致。
- **探索页对齐上游**：6 个栏目（轻小说列表/热门/动画化/今日更新/新书一览/完结全本）、
  首页推荐块、48 个标签，数据全部来自 KMP 层（对齐上游 `Wenku8ExplorePageProvider`）。
- **完整 GBK 解码表**：23940 码位（GB2312 6763 + GBK 扩展繁体/日文汉字），
  修复中文书名/作者乱码。
- **`SYNC.md` 上游同步手册**：上游更新后的映射表 + 同步流程 + 风险清单。
- **`docs/KMP-TROUBLESHOOTING.md` 排错手册**：19 条踩坑记录（现象/原因/解法）。
- **XCUITest UI 测试**：书架/探索/设置 Tab 导航验证 + 真实数据验证。

### Changed

- `BookSourceService` 协议扩展：新增登录能力（`isLoggedIn`/`login`/`logout`/`savedCookie`）、
  `tagCategory`/`fetchBookDetail`，协议改为 class-bound。
- `AppStore` 解耦：登录态、探索、详情、备份全部走协议，不再强转 `Wenku8Service`。
- 自动重试登录：启动快速尝试 + 后台重试 + 探索加载失败自动重登（用户无需手动登录）。
- 探索加载改为后台预加载，不阻塞启动 splash。
- 设置页精简冗余小字说明。

### Fixed

- 中文书名/作者乱码（GBK 解码表不完整）。
- 探索页显示"书目 0 本"（KMP 适配器未实现探索方法）。
- 模拟器启动被登录重试拖慢 31 秒（改为后台重试）。
- UI 测试被 splash 遮罩拦截点击（等 splash 消失再点）。
- 打包时 app 内残留依赖临时路径的动态库（已移除，静态链接进主二进制）。

### Security

- 内置 wenku8 账号仍硬编码（与上游一致）；如担心泄露可改为用户手动登录（UI 入口已留方法，未接 UI）。

## [1.0.0] - 2026-08-24

### Added

- 基于 SwiftUI 的 iOS 轻小说阅读器（iPhone/iPad）。
- 书架、探索、设置三大 Tab。
- 阅读器：仿真卷曲翻页、滚动模式、字体/字号/行距/背景调节、OLED 纯黑夜间模式。
- 离线缓存、EPUB/TXT 导出、阅读统计。
- 封面下采样、异步分页 + 缓存 + 防抖（性能优化）。
