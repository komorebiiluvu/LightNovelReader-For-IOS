# 修复说明

基线：`LightNovelReader-v1.1.0-fixed.zip` 中的完整源码与预编译 `SharedKit.xcframework`。

## 本轮修复

1. **离线插图缓存可跨启动复用**
   - 用 SHA-256 稳定键替代 `String.hashValue`。
   - 读取时自动识别并迁移旧版同章、同序号的缓存文件。

2. **崩溃捕获更安全**
   - 删除会在信号上下文中执行 Foundation、内存分配和普通文件 I/O 的处理器。
   - 保留 `NSException` 记录并衔接已有处理器，报告数量上限为 20。

3. **启动和网络请求减负**
   - 更新检查不再阻塞启动流程。
   - 书架检查最多同时请求两本书。
   - 同一本书的并发目录加载共享同一个任务。
   - 封面磁盘清理移至后台任务。

4. **图片缓存优化**
   - 内存缓存命中后提升为最近使用，避免热点封面被提前淘汰。
   - 封面磁盘键切换到 SHA-256，并按命中迁移旧 DJB2 文件名。

5. **阅读器稳定性与响应速度**
   - 后台分页可感知任务取消，设置连续变化时不再继续无效计算。
   - 页面缓存随渲染参数变化失效，并按真实 LRU 淘汰。
   - 翻页动画增加序列校验和超时解锁，降低交互被永久锁住的风险。

6. **探索“查看全部”无限加载**
   - 展开列表分页切换至 Swift `URLSession` 实现，保留 KMP 用于首页与标签数据。
   - 缓存恢复时同时恢复已加载页数和总页数；网络失败不再静默隐藏。
   - 空页、重复页、总页数倒退均不会推进页码；底部独立哨兵提供稳定的加载和重试入口。

## 验证

- 新增稳定哈希、旧缓存迁移、分页取消、目录请求去重和探索分页异常恢复测试。
- 已执行差异空白检查、源码调用点检查、脚本语法检查和 ZIP 完整性检查。
- 当前执行环境不包含 Xcode/iOS SDK，因此需在 macOS 上再执行一次 `xcodebuild test` 作为发布门禁。

## 后续建议

- 用 MetricKit、PLCrashReporter 或 KSCrash 补齐 native/Swift 崩溃采集。
- 将内置书源凭据从源码中移除，改为用户输入或服务端短期令牌。
- 将大体积状态快照从 `UserDefaults` 拆分到文件或数据库，并保留轻量偏好设置。
