# iOS 性能审计与优化记录

## 目标与边界

本轮重点是探索页“查看全部”的长列表滚动。120 Hz 屏幕每帧预算约为 8.33 ms；任何集中发生在主线程、图片解码或 GPU 纹理上传阶段的工作，都可能表现为掉帧、封面晚出现，或分页接入时的短暂停顿。

本仓库当前环境不能运行 Xcode/Instruments，因此本文区分“已从代码路径确认并修复的瓶颈”和“需要真机量化的数据”，不把静态检查当作实测帧率。

## 原有关键路径

1. `LazyVStack` 创建接近视口的书卡。
2. 书卡为缓存未命中的封面立即创建加载任务。
3. 分页结果追加 20 本书，并额外批量预热其中 8 张封面。
4. 所有磁盘读取、网络等待和图片解码共用一个 FIFO 并发门。
5. 多个请求相近时间完成后，各自立即更新 SwiftUI 状态并创建 GPU 纹理。
6. `exploreBrowse` 发布后，通用 `objectWillChange` 还会调度一次与探索状态无关的完整 App 快照编码和 `UserDefaults` 写入。

## 瓶颈排序

| 优先级 | 瓶颈 | 直接后果 | 本轮处理 |
|---|---|---|---|
| P0 | 可见加载与分页预热叠加 | 新页接入瞬间形成网络、I/O、解码尖峰 | 移除分页批量预热；仅由接近视口的卡片按需加载 |
| P0 | 离屏后底层 detached 任务继续执行 | 快速滚动留下无效任务，与当前屏封面争抢资源 | 为共享任务增加订阅者计数；最后一个订阅者离屏即取消 |
| P0 | 网络、读盘、解码共用 FIFO | 慢网络占满名额时，本地缓存也无法及时显示 | 网络最多 4 路，磁盘/解码最多 2 路，使用独立队列 |
| P0 | 图片同时完成并立即上屏 | 同一帧集中发生状态发布、纹理创建和合成 | 以约 8 ms 间隔分散非内存命中的封面上屏 |
| P1 | 后台预热与可见任务同权 | 用户当前看到的封面可能排在预热任务之后 | 队列区分前台/后台，始终优先可见任务 |
| P1 | 每页重新扫描历史书目构造 `Set` | 页数增加后，主线程去重成本线性增长 | 在浏览状态中增量维护 `bookIDs` |
| P1 | 探索瞬时状态触发完整持久化 | 分页发布约 300 ms 后可能再次造成主线程峰值 | 探索分页状态不再触发 App 快照编码 |
| P2 | 同一批书在一次视图刷新中重复筛选 | 增加 SwiftUI body 的数组遍历与分配 | 单次刷新只计算一份 `visibleBooks` |
| P2 | 封面被连续做两次圆角裁切 | 增加离屏合成/遮罩成本 | 保留 `BookCoverView` 的单层连续圆角 |

## 图片管线的具体策略

- 内存命中：同步返回已经下采样、已经解码的 `UIImage`，不等待异步任务。
- 磁盘命中：在双并发解码通道读取原图并通过 ImageIO 生成最长边 420 px 的缩略图。
- 网络未命中：最多 4 路请求；收到数据后再进入双并发解码通道。
- 展示尺寸：3:4 封面最长边 420 px 时约为 315 × 420 × 4，解码内存约 0.5 MiB；避免把数百万像素原图直接交给列表。
- 写入缓存：内存缓存完成后即可返回 UI；原图磁盘写入在串行后台 actor 中执行，不阻塞封面上屏。
- 快速滚动：探索书卡在缓存未命中时等待 80 ms；如果卡片只是掠过视口，SwiftUI 会先取消任务，网络请求不会启动。
- 批量完成：非内存命中的结果按约一个 120 Hz 帧间隔依次发布，降低单帧纹理上传峰值。

## 保持不变的滚动几何

探索网格继续使用固定列宽、固定卡片高度的显式等高行。分页追加时旧行身份不变，并禁用数据发布引起的隐式布局动画。这样图片优化不会重新引入之前的行缝吸附和页面跳接问题。

## 仍需关注的次级开销

- `AppStore` 仍是覆盖全应用的单一 `EnvironmentObject`。不相关状态变化会使探索页重新求值；进一步拆分 Store 能减少刷新面，但改动范围大，应单独做回归。
- KMP/网页解析层的 HTML 解码和模型转换取决于上游响应规模。它不应在主线程执行；应在 Instruments 的 Time Profiler 中确认实际线程归属。
- SwiftUI/UIKit 的最终图片纹理上传无法完全移出渲染路径。本轮通过下采样、限流和分帧减少峰值，而不是消除该成本。
- 冷缓存首次滚动必然受网络延迟影响；性能目标是保持滚动物理过程流畅，让封面以占位图渐进补齐，而不是等待所有图片完成。

## 真机验证方案

建议使用 Release 构建和支持 ProMotion 的真机，分别测试冷缓存与热缓存：

1. 冷缓存：删除 App 或清理封面缓存，进入“探索 → 全部 → 查看全部”，连续快速滚动至少 10 页。
2. 热缓存：退出后重新进入同一路径并重复滚动。
3. Instruments 使用 Core Animation、Time Profiler 和 Allocations；同时观察网络并发和内存警告。
4. 重点记录分页接入前后 2 秒的 hitches、主线程最重调用栈、图片解码线程以及常驻内存。

验收建议：滚动过程中不出现肉眼可见的页间跳接；分页追加不改变当前 content offset；热缓存时封面应快速出现；冷缓存时即使图片尚未完成，手势和滚动物理也不应被网络/解码阻塞。帧率目标应以设备、系统版本和 Instruments 实测为准。
